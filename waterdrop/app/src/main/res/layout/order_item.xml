<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="4dp"
    android:background="#FFFFFF">

    <!-- This represents one order item in a card format -->
    <androidx.cardview.widget.CardView
        android:id="@+id/orderCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="1dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="8dp"
        android:background="#FFFFFF">

        <!-- LinearLayout containing order details -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <TextView
                android:id="@+id/customerFullNameTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Customer Full Name:"
                android:textColor="#212121"
                android:textSize="16sp"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/productDetailsTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Product Name, Product Quantity:"
                android:textColor="#757575"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/deliveryAddressTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Delivery Address:"
                android:textColor="#757575"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/scheduleDetailsTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Schedule Date, Schedule Time:"
                android:textColor="#757575"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/userIdTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="User ID:"
                android:textColor="#757575"
                android:textSize="14sp" />

            <Spinner
                android:id="@+id/orderStatusSpinner"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:entries="@array/order_status_array"
                android:textColor="#212121" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>

</LinearLayout>
